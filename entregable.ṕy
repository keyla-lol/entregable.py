

students = []
# add

def add_student():
    id = input(" ID: ")
    name = input("name: ")
    age = int(input("age: "))
    course = input("course: ")
    status = input("status(active/inactive): ")
    
    students = {
        "id": id,
        "name": name,
        "age": age,
        "course": course,
        "status": status,
        
    }
    
    students.append(students)
    print("student added")

#show

def show_students():
    if len(students) == 0:
        print(" No students")
    else:
        for s in students:
            print(f"{s['id']} | {s['name']} | {s['age']} | {s['course']} | {s['status']}")
            
# search
def search_student():
    id = input("enter ID: ")
    for s in students:
        if s ["id"] == id:
            print(s)
            return
        print("student not found")
        
#update

def update_student():
    id = input("enter ID op update: ")
    for s in students:
        if s ["id"] == id:
            s ["name"] = input("new name: ")
            s ["age"] = int(input("new age: "))
            s ["course"] = input("new course: ")
            s ["status"] = input("new status: ")
            print("update")
            return
        print("student not found")
        
# delete

def delete_student():
    id = input("enter ID od delete: ")
    for s in students:
        if s ["id"] == id:
            students.remove(s)
            print("delete")
            return
        print("student not found")
        
# menuu

while True:
    print("\n ---menu--- ")
    print("1. add")
    print("2. show")
    print("3. search")
    print("4. update") 
    print("5. delete")   
    print("6. exit ")
    
    option = input("option: ")
    if option =="1":
        add_student()
    elif option == "2":
        show_students()
        
    elif option == "3":
        search_student()
        
    elif option == "4":
        update_student()
        
    elif option == "5": 
        delete_student()
        
    elif option == "6":
        print("byee")
        break
    else: 
        print("invalid option")
                                    
    
        
        
            